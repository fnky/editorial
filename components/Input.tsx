import { styled } from "stitches.config";

export const Input = styled("input", {
  fontFamily: "$sans",
  fontSize: "$2",
  lineHeight: 1,
  border: "1px solid rgba(0, 0, 0, .16)",
  boxShadow: "inset 0 1px 2px 0 rgba(0, 0, 0, .08)",
  borderRadius: "$2",
  padding: "$1 $1",
  margin: 0,
  fontVariantNumeric: "tabular-nums",
});
